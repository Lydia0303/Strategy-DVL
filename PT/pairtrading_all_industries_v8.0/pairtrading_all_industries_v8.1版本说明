# 当前代码的问题：
# 1. 用样本内数据筛选配对
selected_pairs = select_pairs_for_industry(...)  # 用样本内数据

# 2. 用样本外数据回测筛选正收益配对
report = trader.run_backtest(...)  # 用样本外数据

# 3. 用样本外收益筛选正收益配对！这是前视偏差
if report and report['total_return'] > 0:  # 用了未来的信息
    positive_pairs.append(...)

新增功能
实盘回测模式：完全避免前视偏差
三种模式切换：可根据需求选择
保持兼容性：保留v8.0的输出格式
动量因子：仅在权重分配中使用
改进夏普计算：考虑配对相关性

使用方式：
# 如果要使用滚动窗口回测
CONFIG['ROLLING_WINDOW']['enabled'] = True
CONFIG['ROLLING_WINDOW']['mode'] = 'four_year_one_year'

# 如果要用单次回测（保持v8.0逻辑）
CONFIG['ROLLING_WINDOW']['enabled'] = False

# 运行策略
if __name__ == "__main__":
    run_portfolio_backtest_v8_1()