
主要改进点
1、参数逻辑优化
(1)半衰期优化(修复半衰期计算的数学不稳定性（核心）)
| ρ 值  | 半衰期计算             | 约束后结果| 交易含义      |
| ---- | ---------------------- | ------ | --------- |
| 0.50 | -0.693/ln(0.5) = 1.0   | **5**  | 理论极快，但被截断 |
| 0.80 | -0.693/ln(0.8) = 3.1   | **5**  | 快速回归，触发下限 |
| 0.87 | -0.693/ln(0.87) = 5.0  | **5**  | 恰好触及边界    |
| 0.90 | -0.693/ln(0.9) = 6.6   | 6.6    | 正常快速回归    |
| 0.95 | -0.693/ln(0.95) = 13.5 | 13.5   | 中等速度      |
| 0.99 | -0.693/ln(0.99) = 68.9 | **60** | 慢速回归，触发上限 |
所以修改为：
 if 0 < abs(rho) < 1:
                        # 增加极小保护，防止 log(0.999999) 导致分母接近0 → 数值爆炸
                        denominator = np.log(abs(rho))
                        halflife = 15 if abs(denominator) < 1e-6 else -np.log(2) / denominator
避免在 rho 接近 1 时会导致：
np.log(abs(rho)) ≈ 0
halflife = -np.log(2)/0 → 无穷大（爆炸）
（2）波动率因子从 “硬分段” → “连续平滑函数”
原来的代码逻辑是硬分段，问题：0.30 和 0.31 差 0.01，但因子直接跳变 0.2，回测曲线不光滑
现改为：
vol_factor = 0.75 + (volatility / 0.4) * (1.6 - 0.75)
vol_factor = np.clip(vol_factor, 0.75, 1.6)
逻辑完全一样，只是曲线更光滑，泛化能力更强。
