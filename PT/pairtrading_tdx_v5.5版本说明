此版本为多空对冲，纯α策略

趋势过滤的作用与工作原理
趋势过滤在计算Z-Score之后、返回结果之前被调用，其目的不是为了修改Z-Score本身的计算，而是为了生成一个调整交易决策的因子（trend_factor）。
触发条件：
仅当策略初始化时参数 enable_trend_filter=True才会执行。
计算过程​ (self.check_trend(s1_safe.loc[common_dates]))：
函数 check_trend被调用，输入是股票1在共同交易日内的价格序列。
它在该序列上，使用一个时间窗口（由策略参数 tr_filter_window定义，默认为5天）计算简单线性趋势的斜率。
输出两个值：
trend_strength：趋势的强度（标准化后的斜率绝对值）。
trend_direction：趋势的方向（1为上涨，-1为下跌，0为无趋势）。
生成趋势因子​ (trend_factor = self.trend_strength if self.trend_direction != 0 else 0)：
将趋势强度赋值给 trend_factor，但前提是存在明确趋势（方向不为0）。如果没有趋势，则因子为0。
趋势因子的后续用途：
这个 trend_factor会作为函数的最后一个返回值输出。
在 run_backtest函数的主循环中，这个 trend_factor会被传入 get_adaptive_threshold函数。
关键影响：在 get_adaptive_threshold函数内部，trend_factor会用来调整开仓阈值。逻辑是：如果价差本身呈现出较强的趋势（例如，在持续扩大），则收紧开仓阈值，使得策略更早入场，避免“追涨杀跌”。具体代码为 trend_adjustment = 1.0 - 0.2 * trend_factor，趋势越强，阈值乘数变得越小，从而使 entry_threshold降低。
总结趋势过滤的完整逻辑链：
在本函数中：监测股票1的价格趋势，并量化其强度和方向，生成一个 trend_factor。
在交易逻辑中：利用这个因子动态调整交易信号的触发阈值。当价差偏离且伴有强趋势时，降低开仓门槛，促使策略更快地对趋势性偏离做出反应。
因此，趋势过滤是本策略“增强自适应交易带”的一部分，它使策略不仅考虑价差的静态波动率，还考虑价格的动态趋势，从而让交易时机更具适应性。

通达信本地 → 样本内筛选（SSD+协整）→ 初始化卡尔曼 → 样本外动态交易
                    ↓                        ↓
              前N对进入回测 ←──────── 每日更新[μ,γ]
                                          ↓
                                    动态协整检验
                                          ↓
                              有效 ──→ 正常交易
                              失效 ──→ 冷却期/永久剔除