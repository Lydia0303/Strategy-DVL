卡尔曼滤波增强版代码核心改进说明
1. 卡尔曼滤波实现​ (kalman_filter_step方法)
def kalman_filter_step(self, y1, y2):
    """执行一步卡尔曼滤波：预测 + 更新"""
    T = np.eye(2)  # 状态转移矩阵
    Z = np.array([1, y2])  # 观测矩阵
    
    # 预测步骤
    x_pred = T @ self.x
    P_pred = T @ self.P @ T.T + self.Q
    
    # 更新步骤
    y_pred = Z @ x_pred
    S = Z @ P_pred @ Z.T + self.R
    K = P_pred @ Z.T / S
    residual = y1 - y_pred
    self.x = x_pred + K * residual
    self.P = (np.eye(2) - K.reshape(1, -1) @ Z.reshape(-1, 1)) @ P_pred
    
    self.mu, self.hedge_ratio = self.x
    return residual

2. 动态价差计算​ (calculate_spread_zscore_kalman方法)
使用卡尔曼滤波动态估计 μ 和 γ
每次新价格到来时更新状态
价差公式：spread = log(S1) - γ * log(S2)
3. 卡尔曼滤波参数
Q: 状态转移噪声协方差 [1e-5, 1e-5]（参数缓慢变化）
R: 观测噪声协方差 1e-3
P: 状态协方差矩阵（初始为单位矩阵）
x: 状态向量 [μ, γ]
4. 保留原有优化功能
动态协整检验
自适应交易阈值
冷却期（无交易区间）
浮动止损
趋势过滤
失效后净值优化
5. 主程序改进
集成卡尔曼滤波到完整的回测流程
保存对冲比率的动态变化
提供卡尔曼滤波参数优化建议
6. 获取行业成分股路径改为通达信本地板块获取
本地路径为"C:\new_tdx\T0002\export\行业板块.txt"的文件

预期改进效果
更强的适应性: 卡尔曼滤波能够实时跟踪时变的μ和γ，比静态回归更适应市场结构变化
更优的信号: 动态估计的价差能更准确地反映两资产的相对估值
抗噪性: 卡尔曼滤波通过贝叶斯更新，能够过滤观测噪声
记忆性: 通过状态转移矩阵保留历史信息，比滚动窗口更高效

使用建议
首次运行: 保持默认参数
参数调优: 如果对冲比率变化过剧烈，减小Q；如果响应过慢，增大Q
对比分析: 可同时运行卡尔曼版和滚动LS版，对比效果
监控对冲比率: 关注最终对冲比率与初始值的变化，评估市场结构变化
这个完整版本实现了PPT中描述的卡尔曼滤波在配对交易中的应用，可以显著提升策略的动态适应能力。